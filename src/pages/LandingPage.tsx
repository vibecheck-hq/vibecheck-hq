// 1. Add this to the very top of the file, with the other imports:
import SubtextPaywall from '../components/SubtextPaywall';

// 2. Add this inside the return () block where you want the paywall to appear:
<div className="mt-10 mb-10 flex justify-center w-full">
  <SubtextPaywall />
</div>
